server:
  port: 8090

spring:
  application:
    name: FLYWAY-DEMO

  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    baseline:
      -on-migrate: true

